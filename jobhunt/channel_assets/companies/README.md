# Optional company visuals

Place a company image/logo here using the normalized company name, for example:

`ontic.png`

The current banner renderer does not download random images from the web. It only
uses files you provide locally, otherwise it creates a generic professional tech visual.
